import 'package:flutter/material.dart';

class AppColors {
  // Sử dụng tông màu Vàng - Đen (High Contrast tiêu chuẩn cho người khiếm thị)
  static const Color background = Color(0xFF000000); // Đen thuần
  static const Color surface = Color(0xFF121212);
  
  static const Color primary = Color(0xFFFFFF00);    // Vàng chanh (Rất nổi bật trên nền đen)
  static const Color onPrimary = Color(0xFF000000);  // Chữ đen trên nền vàng
  
  static const Color textPrimary = Color(0xFFFFFFFF); // Trắng
  static const Color textSecondary = Color(0xFFB3B3B3);
  
  static const Color error = Color(0xFFCF6679);
}